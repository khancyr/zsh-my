# Add ROS setup

export ROS_OS_OVERRIDE=ubuntu:20.04:focal
source /opt/ros/noetic/setup.zsh

#export ROS_DISTRO=noetic

# Add source 
alias sw="source ./devel/setup.zsh"

if [ -f "./devel/setup.zsh" ]; then
    sw
fi

export PATH="/usr/lib/ccache:$PATH"
